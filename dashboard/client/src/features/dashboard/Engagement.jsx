import { Empty } from "@/components/ui";
export default function Engagement() {
	return (
		<Empty title="Engagement">
			Mood analysis, announcements and celebrations are not built, and are empty in Factor HR too.
		</Empty>
	);
}
