import Simple from "./Simple";

export default function Loans() {
	return (
		<Simple
			title="Loans"
			gap="Loan types, applications, and recovery through payroll."
			note={
				<>
					Frappe HR has Employee Loan and Employee Advance ready to use, but recovery only means
					something once payroll moves. <b>Factor HR’s sub-menu here has never been screenshotted</b>,
					so the pages underneath it are unknown.
				</>
			}
		/>
	);
}
